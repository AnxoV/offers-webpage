import $ from "./utils"

const formElement = $("#login-student-form")
/**formElement.addEventListener("submit", function(event) {
    event.preventDefault();

    const data = new URLSearchParams(new FormData(formElement));
});*/